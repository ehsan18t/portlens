# PortLens - rustfmt configuration
# Enforces consistent formatting across all contributors and AI agents.
# Run: cargo fmt --check (CI) or cargo fmt (auto-fix)

edition = "2024"
max_width = 100
tab_spaces = 4
use_field_init_shorthand = true
use_try_shorthand = true
